services:
  jenkins:
    build:
      context: .
      dockerfile: Dockerfile
    # Native build - architecture auto-detected at build time
    container_name: jenkins
    ports:
      - '9090:8080'
      - '50000:50000'
    volumes:
      - ~/jenkins_home:/var/jenkins_home
      - /var/run/docker.sock:/var/run/docker.sock
      - ~/jenkins_temp:/tmp
    restart: unless-stopped
    mem_limit: 4g
    cpus: 2.0
    shm_size: 1g
